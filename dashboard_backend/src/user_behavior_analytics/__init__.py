"""User behavior analytics package.

This package contains lightweight, in-process utilities for summarizing and analyzing
user interaction events (e.g., clicks, views, searches). It intentionally does not
persist data or emit logs by default to avoid leakage of sensitive information.
"""
